# Local design notes; not part of the plugin (this repo doubles as the
# installed plugin folder, so everything tracked here ships to users).
/exchange/

*.bak
node_modules/

# editor / OS scratch
*~
*.swp
.DS_Store
