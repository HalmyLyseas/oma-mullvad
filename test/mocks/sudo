#!/bin/bash
# PATH-shadowing mock of sudo for scripts/install-mullvad tests: logs the
# exact command it was asked to elevate to $MOCK_LOG and exits 0 without
# ever running it.
set -uo pipefail
echo "sudo $*" >> "${MOCK_LOG:-/dev/null}"
exit 0
